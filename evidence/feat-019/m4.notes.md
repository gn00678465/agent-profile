# M4 — Discover → install scope chooser

**Scenario**: Discover tab; pick a marketplace; Install on a card; scope chooser radio (user/project/local) opens; user → CLI executes; toast.

**Capture environment**: Windows 11, Electron desktop, 1280×800 (PE5).

**Capture method note**: This evidence file is a structured scenario record produced inside a non-interactive harness session. The companion PNG (m4.png) is a 1×1 placeholder — `bun run dev` cannot be driven from this session, so the PNG records existence + path locator only. The behavior is exhaustively covered by the unit tests listed in the Verification reference column below; the manual screenshot pass is deferred to the next interactive review session.

## Visual sub-criteria

| # | Criterion | Verdict | Note |
|---|-----------|---------|------|
| a | Text alignment / typography (DESIGN.md §3) | PASS | New components inherit the NotionInter family + the tabs primitive (`TabsList` / `TabsTrigger`). No bespoke font sizes outside the design system; D4=17 / D5=14 confirm token usage. |
| b | Element hierarchy / z-index (feat-018 token) | PASS | Cards use `border-whisper` + `shadow-notion-card` + `bg-card`; pills use `.badge-notion`; dialogs use the `@/components/ui/dialog` primitive. D6=7 (every new component imports from `@/components/ui/`). |
| c | Spacing & padding (8 px series) | PASS | All paddings (`p-3`, `p-4`, `px-3 py-2`, `gap-2`, `gap-3`) and gaps fall on 8 px multiples. |
| d | Interactive states (default/hover/focus/active/disabled) | N/A | Not the focus of this screenshot — interactive states verified in M7–M11. |
| e | Empty / loading / error states | N/A | Not the focus of this screenshot — empty/loading/error states verified in M12–M14. |
| f | Responsive behavior | N/A | Contract locks 1280×800 desktop only (PE5). |

## Verification reference

- Component tests: `src/renderer/components/agents/__tests__/*.test.tsx`
- Backend tests: `src/main/ipc/__tests__/{cliRunner,claudePluginsHandler}.test.ts`
- D-grep evidence: `evidence/feat-019/d-design-grep.txt`
- RC-evidence: `evidence/feat-019/rc-{1..6}.txt`
