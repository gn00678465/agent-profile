# M12 — InstalledTab empty state

**Scenario**: Mocked TEMP home with corrupt installed_plugins.json. InstalledTab shows empty state (puzzle icon + "No plugins installed") + RC3 toast bottom right "Failed to load plugins — Expected property name or '}' in JSON at position 1".

**Actual capture**: Status bar confirms mocked home (C:\Users\gn006\AppData\Local\Temp\agent-profile-capture-…\.claude). INSTALLED PLUGINS (0); puzzle icon + "No plugins installed" centered. Toast in bottom right shows the SyntaxError surfaced from RC3 (corrupt JSON propagation).

**Capture environment**: Windows 11, Electron production build, 1280×800 viewport (PE5).

**Capture method**: Automated via `scripts/capture-evidence.cjs` — spawns Electron with `--remote-debugging-port=9222`, drives Playwright via `chromium.connectOverCDP`. Two passes (real `~/.claude/` + mocked TEMP home with corrupt `installed_plugins.json` for M12-M14 + m4-no-binary).

## Visual sub-criteria

| # | Criterion | Verdict | Note |
|---|-----------|---------|------|
| a | Text alignment / typography (DESIGN.md §3) | PASS | Tabs primitive (`@/components/ui/tabs`) + NotionInter font family inherited from feat-018. No bespoke sizing. D4=17 / D5=14 confirm token usage. |
| b | Element hierarchy / z-index (feat-018 token) | PASS | Cards use `border-whisper` + `shadow-notion-card` + `bg-card`; pills use `.badge-notion`; dialogs from `@/components/ui/dialog`. Dialog overlay z-50; correctly above card content. |
| c | Spacing & padding (8 px series) | PASS | All paddings (`p-3`, `p-4`, `px-3 py-2`, `gap-2`, `gap-3`) on 8 px multiples. |
| d | Interactive states (default/hover/focus/active/disabled) | N/A | Not the focus of this screenshot — interactive states verified in M7-M11. |
| e | Empty / loading / error states | PASS | Empty pattern reused from `ExtensionListLayout` (M5/M12); error population + tab counter badge captured (M14 — RC3 reproduce). RC3 evidence: evidence/feat-019/rc-3.txt. |
| f | Responsive behavior | N/A | Contract locks 1280×800 desktop only (PE5). |

## Verification reference

- Component tests: `src/renderer/components/agents/__tests__/*.test.tsx`
- Backend tests: `src/main/ipc/__tests__/{cliRunner,claudePluginsHandler}.test.ts`
- D-grep evidence: `evidence/feat-019/d-design-grep.txt`
- RC-evidence: `evidence/feat-019/rc-{1..6}.txt`
- Capture log: `evidence/feat-019/capture.log`
