# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage
playwright-report/
test-results/

# Production
dist
dist-ssr
dist-electron
release
*.local

# Electron
# We want to keep icons in build/
!build/icon.ico
!build/icon.icns
!build/icon.png

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
evidence/

# APM dependencies
apm_modules/

# Claude Code local settings (not the project-tracked .claude/agents.md etc.)
.claude/settings.local.json

# Locally-installed agent assets pulled in via `apm install`.
# Treat like node_modules for agents — declared in apm.yml/apm.lock.yaml,
# regenerated per-clone, not committed.
.claude/skills/
.claude/rules/
.agents/skills/
